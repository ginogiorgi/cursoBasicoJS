var car=["dou", "xd", "lol", "troll"];

if (car.indexOf(licensePlate) == -1){
    var drivingLicense = false;

    car.push(drivingLicense);
    return car;
}
  else{
      var drivingLicense = true;

      car.push(drivingLicense);
      return car;
  }
  